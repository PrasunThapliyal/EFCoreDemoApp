# Ensure consistent line endings
* text=auto eol=lf

# Treat certain files as binary
*.dll binary
*.exe binary
*.pdb binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary

# Mark JSON, YAML, and configs as text for diffs
*.json text
*.yml text
*.yaml text
*.tf text
*.tfvars text
*.hcl text
*.ini text
*.toml text

# C#, JS, TS, HTML, CSS should always normalize to LF
*.cs text diff=csharp
*.js text diff=javascript
*.ts text diff=javascript
*.tsx text diff=javascript
*.jsx text diff=javascript
*.html text diff=html
*.css text diff=css
*.scss text diff=css

# Lock files (keep original line endings)
package-lock.json -text
yarn.lock -text
pnpm-lock.yaml -text
.terraform.lock.hcl -text

# Documentation
*.md text
*.rst text
*.txt text

# Large assets (optional: enable Git LFS if needed)
#*.mp4 filter=lfs diff=lfs merge=lfs -text
#*.zip filter=lfs diff=lfs merge=lfs -text
